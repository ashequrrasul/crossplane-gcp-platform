apiVersion: platform.example.org/v1alpha1
kind: XDatabase
metadata:
  name: dev-database

spec:
  projectID: project-40d105bf-bb2b-4bf6-b9f
  environmentName: dev
  region: us-east1

  networkName: crossplane-dev-vpc
  privateServiceCidr: 10.13.0.0/16

  version: POSTGRES_15
  tier: db-f1-micro

  name: appdb
  user: appuser

  availabilityType: ZONAL
  diskSizeGb: 20
  diskAutoresize: true
  backupEnabled: true
  deletionProtection: false

  passwordSecretNamespace: default
  passwordSecretName: dev-db-password

  providerConfigRef: default